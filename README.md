# Sample Python Project

A starter Python project scaffolded in this workspace.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
python -m pip install -e .
```

## Run

```bash
python -m app.main
```

## Test

```bash
python -m pytest
```
